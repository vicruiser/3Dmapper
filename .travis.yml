language: python

# sudo false implies containerized builds
sudo: false

python:
  - 3.8

install:
   - pip install .

branches:
  only:
      - master

notifications:
  email:
    recipients: 
      - victoria.ruiz.serra@gmail.com
    on_success: never
    on_failure: always